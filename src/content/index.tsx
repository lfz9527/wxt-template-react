

const Content = () => {

    const handleSend = async () => { }

    return (
        <div>
            <button onClick={handleSend}>测试发送消息给background</button>
        </div>
    )
}

export default Content
